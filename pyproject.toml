[project]
name = "microgpt"
version = "0.1.0"
description = "The most atomic way to train and run inference for a GPT"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.4",
    "tinygrad>=0.12.0",
    "torch>=2.11.0",
]
